# This file is a TEMPLATE. Do not put real secret values in Git.
#
# Recommended local workflow (imperative, avoids committing secrets):
#   kubectl create secret generic devops-demo-secret \
#     --namespace devops-demo \
#     --from-literal=APP_DEMO_TOKEN=change-me-locally \
#     --dry-run=client -o yaml | kubectl apply -f -
#
# The placeholder below only exists so `kubectl apply -f k8s/` works out of the box
# for the demo. Replace it (or delete this file and use the command above) before
# using this project for anything real.
apiVersion: v1
kind: Secret
metadata:
  name: devops-demo-secret
  namespace: devops-demo
  labels:
    app: devops-demo
type: Opaque
stringData:
  APP_DEMO_TOKEN: "change-me-locally"
